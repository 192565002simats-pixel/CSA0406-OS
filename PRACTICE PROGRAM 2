#include <stdio.h>
#include <unistd.h>
int main()
{
    if (fork() == 0)
    {
        printf("Child 1 ID = %d\n", getpid());
    }
    else
    {
        if (fork() == 0)
        {
            printf("Child 2 ID = %d\n", getpid());
        }
    }

    return 0;
}
