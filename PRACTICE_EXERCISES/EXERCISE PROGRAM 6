#include <stdio.h>
#include <fcntl.h>
#include <unistd.h>

int main() {
    int fd = open("student.txt", O_CREAT | O_WRONLY, 0644);

    if (fd == -1)
        printf("File not created\n");
    else
        printf("File created successfully\n");

    close(fd);
    return 0;
}
