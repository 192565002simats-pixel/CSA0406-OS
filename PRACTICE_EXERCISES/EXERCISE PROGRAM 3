#include <stdio.h>
#include <unistd.h>

int main() {
    if (fork() == 0) {
        printf("Before exec()\n");
        execl("/bin/ls", "ls", "-l", NULL);
        printf("exec() failed\n");
    }

    return 0;
}
